// task 1   
        // let firstNumber = prompt("Enter the first number:");
        // firstNumber = Number(firstNumber);  // Convert input to a number

        // let secondNumber = prompt("Enter the second number:");
        // secondNumber = Number(secondNumber);  // Convert input to a number

        // let sum = firstNumber + secondNumber;

        // document.write("The sum of " + firstNumber + " and " + secondNumber + " is: " + sum);
    //  task 2
                // let firstNumber = prompt("Enter the first number:");
                // firstNumber = Number(firstNumber); 
        
                // let secondNumber = prompt("Enter the second number:");
                // secondNumber = Number(secondNumber);  
        
                // let sum = firstNumber + secondNumber;
        
                // let difference = firstNumber - secondNumber;
                        
                // let product = firstNumber * secondNumber;
                
                // let modulus = firstNumber % secondNumber;
        
                // document.write("<h3>Results:</h3>");
                // document.write("The sum of " + firstNumber + " and " + secondNumber + " is: " + sum + "<br>");
                // document.write("The difference between " + firstNumber + " and " + secondNumber + " is: " + difference + "<br>");
                // document.write("The product of " + firstNumber + " and " + secondNumber + " is: " + product + "<br>");
                // document.write("The modulus (remainder) of " + firstNumber + " divided by " + secondNumber + " is: " + modulus + "<br>");
        //   task 3
          // a) Declare a variable
    var myVariable;

    // c) Initialize the variable with some number
    myVariable = 5;

    // b) Show the value of the variable in the browser
    document.write("Initial value: " + myVariable + "<br>");

    // d) Increment the variable
    myVariable += 1; // Increment by 1

    // g) Add 7 to the variable
    myVariable += 7;

    // h) Show the value of the variable in the browser
    document.write("Value after increment is: " + myVariable);